using UnityEngine;

[RequireComponent(typeof(Canvas))]
public class BombCanvas : MonoBehaviour
{
    // Pegar a posicao do player
    // Colocar a imagem um pouco para cima
    // Dentro do quadrado a pessoa verifica a bomba e a senha
    // Dar enable no controle de camera ou setas para que a pessoa movimente para colocar a senha
    // Verificar enter colocar o numero
    // Ter um botao de enter para ela confirmar a senha
    // Mostrar o numero da senha



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
